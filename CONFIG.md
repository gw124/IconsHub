# 配置文件说明

## 📋 配置文件概述

IconsHub 使用 `config.yml` 进行配置管理，支持自定义网站信息、部署设置和页脚链接。

### 配置文件位置

**只需要维护一个文件**：`config.yml`（项目根目录）

> **自动同步**：运行 `npm run scan-icons`、`npm run build` 或 `npm run dev` 时，会自动将 `config.yml` 同步到 `public/config.yml`，无需手动复制。

---

## ⚙️ 配置项说明

### 完整配置示例

```yaml
# IconsHub 配置文件
# 用于配置图标库的各种设置

# 网站基本信息
site:
  title: "Icons Hub"
  description: "提供在线图标链接，用于个人NAS设备显示使用，禁止用于商业用途"
  
# GitHub Pages 部署配置
deployment:
  branch: "Web"
  domain: "icons.gw124.top"

# 版权信息配置
copyright:
  startDate: "2025-01-01"
  autoRange: true

# 页脚配置
footer:
  websiteText: "ICONS.GW124.TOP"
  websiteUrl: "https://github.com/GWen124/IconsHub"
  # 注意：Wen 链接已固定在代码中，不可通过配置修改
```

---

## 📖 配置项详解

### site - 网站信息

#### `title` (字符串)
- **说明**: 网站标题，显示在页面顶部
- **默认值**: `"Icons Hub"`
- **示例**: `"我的图标库"`, `"Icon Center"`

#### `description` (字符串)
- **说明**: 网站描述，显示在标题下方
- **默认值**: `"提供在线图标链接，用于个人NAS设备显示使用，禁止用于商业用途"`
- **示例**: `"专业的图标管理平台"`

---

### deployment - 部署配置

#### `branch` (字符串)
- **说明**: GitHub Pages 部署的目标分支
- **默认值**: `"Web"`
- **常用值**: `"gh-pages"`, `"main"`, `"Web"`
- **用途**: GitHub Actions 会将构建结果推送到此分支

#### `domain` (字符串)
- **说明**: 自定义域名（需在 GitHub 仓库设置中配置）
- **默认值**: `"icons.gw124.top"`
- **格式**: 不包含协议（http/https）
- **示例**: `"icons.yourdomain.com"`

**配置自定义域名步骤**：
1. 修改此配置项
2. 在域名 DNS 设置中添加 CNAME 记录
3. 在 GitHub 仓库 Settings > Pages 中配置域名
4. 推送代码触发部署

---

### copyright - 版权信息

#### `startDate` (字符串)
- **说明**: 版权开始年份
- **格式**: `"YYYY-MM-DD"`
- **默认值**: `"2025-01-01"`
- **示例**: `"2024-01-01"`

#### `autoRange` (布尔值)
- **说明**: 是否自动显示年份范围
- **默认值**: `true`
- **效果**:
  - `true`: 显示 `2025-2026`（如果当前是 2026 年）
  - `false`: 只显示 `2025`

---

### footer - 页脚配置

#### `websiteText` (字符串)
- **说明**: 网站名称链接的显示文本
- **默认值**: `"ICONS.GW124.TOP"`
- **示例**: `"我的网站"`, `"Icon Hub"`

#### `websiteUrl` (字符串)
- **说明**: 网站名称的跳转链接
- **默认值**: `"https://github.com/GWen124/IconsHub"`
- **格式**: 完整的 URL（包含 `https://`）
- **示例**: `"https://your-website.com"`

#### ⚠️ 固定配置（不可修改）

**作者链接**已固定在代码中：
- 文本: `"Wen"`
- 链接: `"https://gw124.top/"`

这是为了保护原作者信息，符合开源协议要求。

---

## 🔄 配置同步机制

### 自动同步

运行以下任一命令时，会自动同步配置：

```bash
npm run scan-icons    # 扫描图标时同步
npm run build         # 构建时同步
npm run dev           # 开发时同步
```

### 同步内容

```
config.yml (根目录)
    ↓ 自动复制
public/config.yml (运行时读取)
```

### 实现原理

在 `scripts/scan-icons.js` 中：

```javascript
// 复制 config.yml 到 public 目录
const configSource = path.join(__dirname, '../config.yml');
const configPublic = path.join(__dirname, '../public/config.yml');
if (fs.existsSync(configSource)) {
    fs.copyFileSync(configSource, configPublic);
    console.log('⚙️  已同步 config.yml 到 public 目录');
}
```

---

## 📝 修改配置的步骤

### 步骤 1: 编辑配置文件

```bash
vim config.yml
# 或使用你喜欢的编辑器
```

### 步骤 2: 验证配置（可选）

```bash
npm run config:validate
```

### 步骤 3: 同步到 public

**方式 A: 自动同步（推荐）**
```bash
npm run scan-icons
```

**方式 B: 手动复制**
```bash
cp config.yml public/config.yml
```

### 步骤 4: 测试配置

**开发环境**：
```bash
npm run dev
# 访问 http://localhost:8080 查看效果
```

**生产构建**：
```bash
npm run build
# 检查 dist 目录
```

### 步骤 5: 部署更新

```bash
git add config.yml
git commit -m "chore: 更新配置"
git push origin main
```

GitHub Actions 会自动部署新配置。

---

## 💡 最佳实践

### 1. 只修改根目录的 config.yml

❌ **不要直接修改** `public/config.yml`  
✅ **只修改** 根目录的 `config.yml`

**原因**：
- `public/config.yml` 会被自动覆盖
- 根目录的文件才是源文件
- 避免配置不一致

### 2. 修改后重新扫描

```bash
# 修改配置后
npm run scan-icons
```

这会确保配置同步到 public 目录。

### 3. 版本控制

只提交根目录的 `config.yml`：

```bash
git add config.yml
git commit -m "配置更新"
```

`public/config.yml` 会在构建时自动生成，不需要提交。

### 4. 配置验证

修改配置后运行验证：

```bash
npm run config:validate
```

确保配置格式正确。

---

## 🔧 配置示例

### 示例 1: 修改网站标题和描述

```yaml
site:
  title: "我的图标中心"
  description: "为 NAS 和自建服务提供高质量图标"
```

### 示例 2: 配置自定义域名

```yaml
deployment:
  branch: "gh-pages"
  domain: "icons.mydomain.com"
```

**注意**：还需要配置 DNS 和 GitHub Pages 设置。

### 示例 3: 修改版权信息

```yaml
copyright:
  startDate: "2024-06-01"
  autoRange: true
```

显示效果：`Copyright © 2024-2025`（如果当前是 2025 年）

### 示例 4: 自定义 Footer 链接

```yaml
footer:
  websiteText: "访问我的博客"
  websiteUrl: "https://blog.yourdomain.com"
```

---

## 🐛 常见问题

### Q1: 修改配置后没有生效？

**解决方法**：
```bash
# 1. 重新扫描（自动同步配置）
npm run scan-icons

# 2. 重启开发服务器
# Ctrl+C 停止，然后
npm run dev

# 3. 强制刷新浏览器
# Ctrl+Shift+R (Windows)
# Cmd+Shift+R (Mac)
```

### Q2: config.yml 格式错误？

**检查方法**：
```bash
npm run config:validate
```

**常见错误**：
- 缩进不正确（使用 2 个空格）
- 冒号后没有空格
- 值没有用引号包裹

**正确格式**：
```yaml
site:
  title: "My Title"    # ✅ 冒号后有空格，值有引号
  
footer:
websiteText: "Text"    # ❌ 缩进不正确
websiteUrl:"url"       # ❌ 冒号后没有空格
```

### Q3: 为什么有三个地方的配置？

**只需要维护一个**：`config.yml`（根目录）

其他两个是自动生成的：
- `public/config.yml` - 运行时自动同步
- `src/utils/configLoader.ts` 中的默认值 - 配置加载失败时的后备

### Q4: 如何恢复默认配置？

删除 `config.yml`，系统会使用代码中的默认配置：

```bash
rm config.yml
npm run dev
```

或者参考本文档的默认值重新创建。

---

## 📊 配置优先级

```
1. config.yml（根目录）
   ↓ 自动同步
2. public/config.yml（运行时读取）
   ↓ 如果加载失败
3. 代码默认值（后备配置）
```

**建议**：
- 始终维护根目录的 `config.yml`
- 运行 `npm run scan-icons` 确保同步
- 不要手动修改 `public/config.yml`

---

## 🎯 配置工作流

### 日常修改

```bash
# 1. 编辑配置
vim config.yml

# 2. 自动同步
npm run scan-icons

# 3. 测试
npm run dev

# 4. 提交
git add config.yml
git commit -m "更新配置"
git push
```

### 部署前检查

```bash
# 1. 验证配置
npm run config:validate

# 2. 扫描图标（会自动同步配置）
npm run scan-icons

# 3. 构建
npm run build

# 4. 推送部署
git push origin main
```

---

## 📚 相关文档

- [README.md](README.md) - 项目概述
- [GUIDE.md](GUIDE.md) - 详细使用指南
- [DEPLOY.md](DEPLOY.md) - 部署指南

---

**配置很简单，只需要维护一个 `config.yml` 文件！** 🎉

