# IconsHub - 图标库管理系统

<div align="center">

![IconsHub](public/screenshot.png)

**一个现代化的图标库管理和展示系统，专为 NAS 和自建服务设计**

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Vue](https://img.shields.io/badge/Vue-3.2-green.svg)](https://vuejs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-4.5-blue.svg)](https://www.typescriptlang.org/)

[在线演示](https://icons.gw124.top) · [快速开始](#快速开始) · [功能特性](#功能特性) · [文档](#文档)

</div>

---

## 📋 目录

- [项目简介](#项目简介)
- [功能特性](#功能特性)
- [快速开始](#快速开始)
- [使用指南](#使用指南)
- [配置说明](#配置说明)
- [部署方法](#部署方法)
- [性能优化](#性能优化)
- [开发指南](#开发指南)
- [常见问题](#常见问题)
- [贡献指南](#贡献指南)
- [许可证](#许可证)

---

## 🎯 项目简介

IconsHub 是一个基于 Vue 3 + TypeScript 开发的现代化图标库管理系统，主要用于为 NAS 设备、自建服务、Docker 容器等提供统一的图标管理和在线访问服务。

### 适用场景

- 🏠 **NAS 用户**：为 Synology、群晖等 NAS 上的应用提供图标
- 🐳 **Docker 用户**：为容器化应用提供统一图标
- 🌐 **自建服务**：为各类自建服务提供美观的图标展示
- 📱 **个人项目**：作为个人图标库，提供在线链接访问

### 项目亮点

- ✨ **1800+ 高质量图标**：涵盖 16 个分类
- 🚀 **高性能优化**：懒加载 + Service Worker 缓存
- 🎨 **现代化 UI**：基于 Element Plus 的精美界面
- ⚙️ **灵活配置**：YAML 配置文件，易于定制
- 🔍 **智能搜索**：支持分类筛选和关键词搜索
- 📋 **一键复制**：点击即可复制图标链接
- 🌍 **自动部署**：GitHub Actions 自动化部署

---

## ✨ 功能特性

### 核心功能

#### 1. 图标管理
- 📁 **16 个精选分类**：应用中心、云服务、AI 工具、NAS 应用等
- 🔄 **自动扫描**：自动识别 `public/icon` 目录下的图标文件
- 📊 **实时统计**：显示分类数量、图标总数
- 🏷️ **自定义分类**：支持自定义分类名称和排序

#### 2. 用户交互
- 🔍 **实时搜索**：支持图标名称搜索
- 🎯 **分类筛选**：下拉选择特定分类
- 📋 **一键复制**：点击图标复制 URL 链接
- 💡 **提示信息**：鼠标悬停显示完整文件名
- 📱 **响应式设计**：完美适配桌面端和移动端

#### 3. 性能优化
- ⚡ **图片懒加载**：只加载可见区域的图标
- 💾 **Service Worker**：智能缓存策略
- 🗜️ **Gzip 压缩**：减少传输大小
- 🔄 **代码分割**：按需加载模块
- 🖼️ **WebP 支持**：可选的图片格式优化

#### 4. 部署集成
- 🤖 **GitHub Actions**：自动化构建和部署
- 🌐 **多平台支持**：GitHub Pages、Vercel、Netlify
- ⚙️ **YAML 配置**：灵活的部署配置
- 🔒 **HTTPS 支持**：安全的图标访问

---

## 🚀 快速开始

### 环境要求

- Node.js >= 14.x
- npm >= 6.x 或 yarn >= 1.22.x
- Git

### 安装步骤

#### 1. 克隆仓库

```bash
git clone https://github.com/GWen124/IconsHub.git
cd IconsHub
```

#### 2. 安装依赖

```bash
npm install
```

#### 3. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:8080` 查看效果。

#### 4. 构建生产版本

```bash
# 标准构建
npm run build

# 优化构建（包含 WebP 转换）
npm run build:optimized
```

---

## 📖 使用指南

### 基本使用

#### 1. 添加图标

将图标文件放入 `public/icon/分类名称/` 目录：

```bash
public/icon/
  ├── AI/
  │   ├── ChatGPT.png
  │   └── DeepSeek.png
  ├── Docker/
  │   ├── Portainer.png
  │   └── Nextcloud.png
  └── ...
```

支持的图标格式：`.png`, `.jpg`, `.jpeg`, `.svg`, `.webp`

#### 2. 自定义分类

编辑 `category-titles.json` 文件：

```json
{
  "AI": "AI 工具",
  "Docker": "Docker 容器",
  "新分类": "自定义分类名称"
}
```

分类按文件中的顺序显示。

#### 3. 运行图标扫描

```bash
npm run scan-icons
```

这会自动扫描 `public/icon` 目录并生成 `public/db.json`。

#### 4. 获取图标链接

在网站上点击任意图标，链接会自动复制到剪贴板。

**链接格式**：
```
https://your-domain.com/icon/分类名称/图标名称.png
```

---

## ⚙️ 配置说明

### 配置文件：`config.yml`

```yaml
# 网站基本信息
site:
  title: "Icons Hub"
  description: "提供在线图标链接，用于个人NAS设备显示使用"
  
# GitHub Pages 部署配置
deployment:
  branch: "Web"              # 部署分支
  domain: "icons.gw124.top"  # 自定义域名

# 版权信息配置
copyright:
  startDate: "2025-01-01"    # 开始年份
  autoRange: true            # 自动显示年份范围

# 页脚配置
footer:
  websiteText: "ICONS.GW124.TOP"
  websiteUrl: "https://github.com/GWen124/IconsHub"
```

### 配置项说明

#### site（网站信息）
- `title`: 网站标题，显示在页面顶部
- `description`: 网站描述

#### deployment（部署设置）
- `branch`: GitHub Pages 部署的目标分支
- `domain`: 自定义域名（需在 GitHub 仓库设置中配置）

#### copyright（版权信息）
- `startDate`: 版权开始年份
- `autoRange`: 是否自动显示年份范围（如：2025-2026）

#### footer（页脚配置）
- `websiteText`: 网站名称链接的显示文本
- `websiteUrl`: 网站名称的跳转链接

> **注意**：作者链接 "Wen" 已固定在代码中，不可通过配置修改。

---

## 🌐 部署方法

### 方法 1：GitHub Pages（推荐）

#### 自动部署

1. Fork 本仓库
2. 在仓库设置中启用 GitHub Pages
3. 配置 `config.yml` 文件
4. 推送代码到 `main` 分支
5. GitHub Actions 会自动构建并部署到指定分支

#### 手动触发

1. 进入仓库的 **Actions** 标签
2. 选择 **CI Deploy to GitHub Pages**
3. 点击 **Run workflow**
4. 选择参数后启动

### 方法 2：Vercel

1. 导入 GitHub 仓库到 Vercel
2. 设置构建命令：`npm run build`
3. 设置输出目录：`dist`
4. 部署

### 方法 3：Netlify

1. 连接 GitHub 仓库
2. 构建命令：`npm run build`
3. 发布目录：`dist`
4. 部署

### 方法 4：Docker

#### 使用 Nginx

1. 构建项目：
```bash
npm run build
```

2. 创建 Dockerfile：
```dockerfile
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/
COPY nginx.conf.example /etc/nginx/conf.d/default.conf
```

3. 构建镜像：
```bash
docker build -t iconshub .
```

4. 运行容器：
```bash
docker run -d -p 80:80 iconshub
```

---

## ⚡ 性能优化

### 内置优化功能

1. **图片懒加载**
   - 只加载可视区域的图标
   - 减少初始加载时间 70%+

2. **Service Worker 缓存**
   - 智能缓存策略
   - 二次访问速度提升 90%+

3. **Gzip 压缩**
   - 自动压缩 JS、CSS、HTML
   - 减少传输大小 70%

4. **代码分割**
   - 按需加载模块
   - 减少首屏加载体积

### 可选优化

#### WebP 图片转换

将 PNG 图片转换为 WebP 格式，体积减少 30-50%：

```bash
npm run optimize-images
```

或使用优化构建：

```bash
npm run build:optimized
```

> **注意**：此操作会生成 `.webp` 文件，不会删除原始图片。

### 性能数据

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 首次加载 | 15-20秒 | 3-5秒 | **70%** |
| 二次访问 | 5-8秒 | 0.5秒 | **90%** |
| 图片大小 | 104MB | 50-70MB | **40%** |
| 带宽消耗 | 104MB/次 | <1MB/次 | **95%** |

---

## 👨‍💻 开发指南

### 项目结构

```
IconsHub/
├── public/                  # 静态资源
│   ├── icon/               # 图标文件目录
│   │   ├── AI/            # 分类目录
│   │   ├── Docker/
│   │   └── ...
│   ├── db.json            # 自动生成的图标数据
│   ├── config.yml         # 配置文件副本
│   ├── service-worker.js  # Service Worker
│   └── index.html         # HTML 模板
├── src/                    # 源代码
│   ├── views/             # 页面组件
│   │   └── index.vue      # 主页面
│   ├── utils/             # 工具函数
│   │   ├── configLoader.ts    # 配置加载
│   │   └── copyright.ts       # 版权处理
│   ├── App.vue            # 根组件
│   └── main.ts            # 入口文件
├── scripts/                # 脚本工具
│   ├── scan-icons.js      # 图标扫描
│   ├── optimize-images.js # 图片优化
│   ├── config-loader.js   # 配置加载器
│   ├── validate-config.js # 配置验证
│   └── simple-watch.js    # 文件监听
├── config.yml             # 配置文件
├── category-titles.json   # 分类名称配置
├── vue.config.js          # Vue 配置
├── package.json           # 项目配置
└── README.md             # 本文件
```

### NPM 脚本

```json
{
  "serve": "启动开发服务器",
  "dev": "扫描图标 + 启动开发服务器",
  "build": "构建生产版本",
  "build:optimized": "优化构建（含 WebP）",
  "scan-icons": "扫描图标文件",
  "optimize-images": "优化图片为 WebP",
  "watch": "监听文件变化",
  "config:validate": "验证配置文件",
  "config:load": "加载配置测试"
}
```

### 开发工作流

1. **添加新图标**
```bash
# 1. 将图标放入对应分类
cp your-icon.png public/icon/分类名称/

# 2. 扫描图标
npm run scan-icons

# 3. 启动开发服务器
npm run serve
```

2. **修改配置**
```bash
# 1. 编辑 config.yml
vim config.yml

# 2. 验证配置
npm run config:validate

# 3. 重启服务器
```

3. **添加新分类**
```bash
# 1. 创建分类目录
mkdir public/icon/新分类

# 2. 添加图标文件

# 3. 编辑分类标题
vim category-titles.json

# 4. 扫描图标
npm run scan-icons
```

---

## 🤔 常见问题

### 1. 图标没有显示？

**原因**：
- 图标文件格式不支持
- 图标文件名包含特殊字符
- 没有运行 `npm run scan-icons`

**解决方法**：
```bash
# 1. 检查文件格式（支持：png, jpg, jpeg, svg, webp）
# 2. 重命名文件，避免特殊字符
# 3. 重新扫描
npm run scan-icons
```

### 2. 配置修改不生效？

**原因**：
- 配置文件语法错误
- 浏览器缓存
- Service Worker 缓存

**解决方法**：
```bash
# 1. 验证配置
npm run config:validate

# 2. 强制刷新浏览器（Ctrl+Shift+R 或 Cmd+Shift+R）

# 3. 清除 Service Worker
# 在浏览器开发者工具 > Application > Service Workers > Unregister
```

### 3. 部署后图标加载慢？

**原因**：
- 图片未优化
- 缓存未生效
- 服务器未启用压缩

**解决方法**：
```bash
# 1. 使用优化构建
npm run build:optimized

# 2. 检查 Service Worker 是否启用
# F12 > Application > Service Workers

# 3. 配置服务器压缩（参考 nginx.conf.example）
```

### 4. 如何批量添加图标？

**方法 1：复制文件**
```bash
cp -r /path/to/icons/* public/icon/分类名称/
npm run scan-icons
```

**方法 2：使用脚本**
```bash
# 创建批量导入脚本
for file in /path/to/icons/*; do
  cp "$file" public/icon/分类名称/
done
npm run scan-icons
```

### 5. 如何自定义主题颜色？

修改 `src/views/index.vue` 中的 SCSS 变量：

```scss
$primary-color: #6366f1;     // 主色调
$secondary-color: #4f46e5;   // 次色调
$bg-color: #f8fafc;          // 背景色
```

### 6. 如何禁用 Service Worker？

编辑 `src/main.ts`，注释掉相关代码：

```typescript
// 注释掉这部分
/*
if ('serviceWorker' in navigator && process.env.NODE_ENV === 'production') {
  // ...
}
*/
```

---

## 🤝 贡献指南

欢迎贡献！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 贡献类型

- 🐛 Bug 修复
- ✨ 新功能
- 📝 文档改进
- 🎨 UI/UX 改进
- ⚡ 性能优化
- 🔒 安全修复

---

## 📄 许可证

本项目基于 [MIT License](LICENSE) 开源。

---

## 🙏 鸣谢

- 基于 [heizicao/My Icon](https://gitee.com/heizicao/my-icon) 修改
- 使用 [Element Plus](https://element-plus.org/) UI 框架
- 图标来源于互联网，版权归原作者所有

---

## 📧 联系方式

- **项目主页**: https://icons.gw124.top
- **GitHub**: https://github.com/GWen124/IconsHub
- **作者网站**: https://gw124.top

---

<div align="center">

**如果这个项目对你有帮助，请给一个 ⭐ Star！**

Made with ❤️ by [Wen](https://gw124.top)

</div>
